.toUpperCase - выводит строчные буквы все с БОЛЬШОЙ буквы

.toLowerCase - выводит строчные буквы все с МАЛОЙ буквы

.toFixed(1) - округление до одного знака после запятой

.trim() - убирает пробелы слева и справа

.toString() - преобразовывает числа в строку

.slice(5) - извлекает часть строки и возвращает новую строку без изменения оригинальной строки (в данном случае от 5 символа и до конца строки)
.slice(5) - извлекает часть строки и возвращает новую строку без изменения оригинальной строки (в данном случае от 5 символа и 19 символа строки)




